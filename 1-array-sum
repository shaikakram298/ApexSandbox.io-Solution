public static Integer arraySum(List<Integer> numbers) {
    //code here
    Integer sum=0,n=numbers.Size();
    if(n!=null){
        for(Integer i=0;i<n;i++){
           sum+=numbers[i];
        }

    }
    return sum;
}