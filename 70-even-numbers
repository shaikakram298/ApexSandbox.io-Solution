public List<Integer> evenNumbers(Integer n) {
    //code here
    List<Integer> evenList=List<Integer>();
    for(Integer i=0;i<n;i++){
        if(i/2==0){
            evenList.add(i);
        }
    }
    return evenList;
}